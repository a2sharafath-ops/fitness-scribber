# A32 — engineering completion and hosted verification

8 September 2026. Owner directly requested: “ok complete the engineering work and hosted verification. you have all my approvals, dont pause in between complete them both in one go.”

This extends A30/A31 for the remaining engineering fixes and fictional hosted verification on the same Supabase project `haxxetirrcrwzwdzsdui`, Vercel project `fitness-scribber-kq6i`, and `codex/exercise-pooling-local` branch. It includes normal local commits, branch pushes/automatic protected Previews, scoped corrective migrations and the six pooling Edge Functions, new fictional test identities and their non-destructive retirement. The previous ten-build/six-identity allowance is exhausted; this is a new explicitly authorized verification attempt, not permission to reuse retired accounts.

Conservative execution bounds for this attempt: at most four additional Preview builds, three additional fictional Auth identities, 1,000 new application rows, 800 harness requests, no new Storage objects, and no more than two simultaneous mutating requests. Included provider capacity only; additional spend remains ₹0. These are agent-selected safeguards under the requested extension, not provider quota guarantees. Use the minimum resources needed; reconcile uncertain writes before retrying.

Preserve all existing records, original coach accounts, their existing fictional test window, Classic, main and the current Production alias. Global pooling stays off. Any necessary test enrollment expansion must retain the per-workspace 160-write cap and fixed expiry; never enroll a real client. Rehearse checksummed schema changes against a fresh verified backup before applying. Temporary authority is revoked and new test accounts are disabled after testing, while history remains.

## Work to close

1. Complete deterministic context, inventory/loading, budget, timing, ranking, source-lineage and proposal/diff engineering fixtures.
2. Verify all supported programming entry points preserve the unassigned-draft/explicit-approval boundary.
3. Exercise hosted stale context, concurrency, exact retries, failed/unknown saves, authentication changes and execution-history preservation.
4. Verify R1/R2/R3 services and the final protected Preview against the actual hosted backend.
5. Complete available keyboard/reflow/zoom checks, reduce avoidable bundle loading and record bounded performance measurements.
6. Verify feature-off/Classic compatibility, backup preservation and final containment.
7. Reconcile the 82-task sprint and 110-case matrix with evidence at the actual layer tested. Keep human, clinical, privacy and unsupported-provider acceptance separate.

This is not authorization for main/Production promotion, real-client enablement, paid services, invitations, professional signatures, content-rights acceptance, optional device/clinical integrations, destructive restore or off-device backup to an unspecified destination. No claim of human UAT, real-device testing or accepted clinical output may be inferred from synthetic tests. Unexpected access, preservation or cost failures remain safety stops; ordinary fixes do not need repeated approval.

## Explicit shared-backend confirmation

The system rejected Edge deployment twice because the shared-service impact required direct confirmation. No alternate deployment or execution route was used. The owner then answered **“yes”** to: “May I update the six pooling functions in the shared Supabase test backend, knowing this can affect the current app’s backend behavior? I’ll preserve main and the Production frontend, keep global pooling off, retain all records and original accounts, and make no real-client or paid-service changes.” This resolves that exact deployment approval dependency, not the later launch gates.

Browser transport verification uses a loopback-only proxy restricted to new A32 identities: up to 1,200 proxied requests and 300 write requests, still within the application-row and per-workspace limits. Faults drop responses only after a real hosted commit, interrupt requests before forwarding, or briefly hold an approval for a controlled race. This is simulated transport failure, not a claim of physical network/device testing.
