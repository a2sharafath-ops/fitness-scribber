# Proposed bounded hosted test expansion

8 September 2026 — **not executed; exact shared-backend expansion requires confirmation.** The current owner request authorizes fixing the remaining work; local fixes are recorded in [A33 evidence](../operations/A33_ENGINEERING_FIXES.md). This package identifies the newly discovered shared-test capacity/scope change, rather than re-requesting existing product or engineering approval.

## Proposed action

Use the same Supabase project `haxxetirrcrwzwdzsdui` and protected Vercel branch Preview. Preserve main, Production, global pooling-off, all existing records, the two original accounts and their reserved slots/window. No new paid service, real client, invitation, clinician signature or live exercise content.

1. Export a fresh private backup and rehearse a checksummed, additive test-setup migration locally before shared application.
2. Increase the test-workspace lifetime allowance from six to **nine**: four historical workspaces + three new engineering workspaces + two original-coach reservations. Preserve every historical workspace and its revocation.
3. Permit exactly two bounded workspaces for one **new engineering coach**, plus one workspace for a second new engineering account. Keep original-coach creation/status behavior single-workspace and deterministic. Do not generally remove the uniqueness/scope protection for every coach; use explicit engineering-fixture slot handling and test its authorization.
4. Keep the per-workspace 160-write quota, stop reserve and existing window end (`2026-09-15T10:37:56.492Z`). No expiry extension. At most two new Auth accounts, three new workspaces, 1,000 added application rows, 1,200 combined remote test requests, 400 write-method requests, two simultaneous mutations, two additional protected Preview builds, zero Storage objects and ₹0 additional spend. Stop before any limit or unexpected preservation failure.
5. Test positive same-coach cross-client/template/copy-last journeys, numerical-review dropped responses, real Auth expiry/refresh/sign-out/account switch with pending journals, travel history and exact kg/inventory paths. Publish only the reviewed code to the protected test Preview; never promote main/Production as part of this package.
6. Reconcile uncertain operations by their original keys. Revoke new workspaces and fixture manifests/notices/grants, sign out and disable the two new accounts, retain all history, restore the original test allowlist and verify original rows/accounts/window remain unchanged.

This is an expansion of the shared backend's **test setup**, and could affect test-backend behavior. It is not a new production release or approval of clinical content. If the reserved/active state changed since preflight, re-inspect and adjust the proposed bounds before action; do not consume original reservations.

## Separate information still needed for release

- Business/coach name, contact and actual business establishment; exact initial markets and service scope.
- Applicable exercise-policy/clinical reviewer identities, accepted executable versions/parameters and content-rights evidence. The six PRDs and twelve catalogue owner reviews stay accepted.
- Actual coach/client participants and physical devices/browser matrix for acceptance; agreed performance targets and operator/support ownership.
- A named private off-device backup destination and authorization for the exact managed-service restore target. No personal data may be uploaded to an unspecified destination.
- Final privacy/consent/retention/rights/incident decisions, limited pilot and later Production enablement decisions. Optional clinical/device/sharing features can remain excluded from the initial launch.

The assistant cannot supply these real-world facts or sign on another person's behalf. Until supplied, associated features and release actions remain gated.
