# A31 final coach-test verification

8 September 2026. **Usable protected fictional coach-testing build verified. One engineering-account cleanup action is blocked pending direct owner confirmation; full release acceptance remains open.**

## Open and test

- [Stable coach-testing entry](https://fitness-scribber-kq6i-git-codex-exercise-pool-3bc1c9-cureocity1.vercel.app/pooling-test)
- [Exact tested build](https://fitness-scribber-kq6i-13aibvveb-cureocity1.vercel.app/pooling-test)
- Application commit: `10dea53ca4a4ad819b574f7b3bfa60f868e742ba`; GitHub Preview deployment `6326396293`; Vercel deployment `8VYvCuJZ5E8PhXhDu5qn4Zhn9mD7`. GitHub deployment and commit statuses both report success.
- [Step-by-step guide](./COACH_PREVIEW_TEST_GUIDE.md)

Use existing authorized Vercel access, then an existing application coach login. Both original coach profiles were checked read-only: still coaches, not banned, present in the allowlist. No original password/role was changed. Each may create one NEW clearly fictional workspace. No real client record is enrolled automatically.

The fixed window ends **15 September 2026, 16:07:56 IST / 10:37:56 UTC**. Global R1/R2/R3 remain false; only explicit test enrollments enable the new workflow. The three browser flags are true for the implementation-branch Preview only. Production variables and protection were not changed. No real exercise or clinical content is admitted by this testing scope.

## Actual final-build evidence

Normal application login passed on both the exact build and the stable branch alias. The updated guide appeared on both. All actions below used the normal UI and hosted backend under the new run-owned fictional coach identity, not browser-injected backend state or an original coach's credentials.

1. Preparation returned unassigned draft 29, context generation 30. Its current source review and reviewed baseline occurrence identity were preserved.
2. Exact validation returned decision 24, ready for coach review, no gaps: fictional two-set, one-second-per-set placeholder, no external load. Approval was disabled before validation and before the explicit review acknowledgement.
3. Explicit approval saved assignment 12. Start was disabled until the current health-change response was explicitly selected. Start and an actual zero saved.
4. Early revocation affected only that engineering workspace. New preparation became disabled. The active session and historical records remained visible.
5. Stop remained enabled after revocation and persisted. An attributed one-second correction then saved with the original performed time and an explicit fictional correction reason. The original zero remained visibly superseded-but-retained, beside the current correction. Refresh preserved both records. No physical exercise was performed.
6. Sampled final desktop DOM had no alerts or page-width overflow at 1280 px. This is not a full accessibility/device certificate.

The preceding A31 browser/API evidence covers generation and compatible swap, zero/correction, fresh-answer pause/resume/completion, R2 no-change for completed occurrences and a separate unstarted-baseline proposal, R3 comparable-history progression, and protected Preview two-session exact weekly approval. Weekly review 4 assigned drafts 24 and 27; both appeared in the persisted client list. The [progress report](./COACH_TEST_PROGRESS.md) distinguishes these layers and records harness corrections. Current code has **141 passing unit tests**, clean lint and a successful build; the pre-existing large-bundle warning remains.

## Final preservation and resources

Read-only verification at **2026-09-08T11:28:44.638Z**:

| Check | Result |
|---|---|
| Pre-A31 records | All 711 rows across 83 public/auth/storage tables preserved unchanged |
| Additional retained rows in those baseline tables | 123; counted separately from the protected baseline |
| New enrollment infrastructure | One config row and one engineering workspace row |
| Workspace writes | 132 of 160; normal-action threshold 140; Stop/result reserve retained |
| Worst-case approved application-row bound | 940 including both original coaches' future bounded workspaces, below 1,000 |
| Auth identities created | Six lifetime run identities; no seventh |
| Preview builds | Ten of ten; no further deployment triggered for this final receipt |
| Storage / real catalogue publication | Zero Storage objects; zero real published candidates |
| Global pooling flags | R1 false, R2 false, R3 false |
| Current Production and remote main | Unchanged `5de01b4e960e03e28d4a8b22527937103de10ad6`; Production deployment remains `6119996170` |
| Local main / Fitness Scribber Classic | Unchanged `08ac0673d6e1d67ccd9fee2ffc5995074bcbf162`, tag `fitness-scribber-classic-2026-09-07` |

Fresh pre-migration backup SHA-256 `08e4d30cf37d6c74aafcba210ff2dd6af806d29f063f2732f41404362e04eaee`, 707,482 bytes. The 83-table native restore and exact two-pass migration rehearsal passed before hosted application. The migration and transaction checksums are in the progress report. Backups, credentials and detailed evidence stay in ignored, restricted local directories; no secret or backup upload occurred. This is same-device relational recovery, not a full managed-service/off-device restore certificate.

The task's local Vite process and exact temporary native PostgreSQL restore process were stopped; restored data and backup files remain. Approximately 2.3 GiB free was measured at the final check. No unrelated process, account or file was deleted. The hosted Preview remains available without a local server.

## Exact cleanup blocker — do not bypass

The system's auto-review rejected `coach-test-retire-engineering.mjs` before execution, including a second request after read-only ownership verification. It requires direct user authorization for hosted account/permission cleanup; the recorded A30/A31 approval documents were not accepted by that gate as direct authorization. No alternate API/UI route was used to bypass the rejection.

Read-only proof: the sixth identity has the exact private run marker and fictional A31 metadata; it owns the already revoked engineering workspace and is not either original coach. All five older engineering users remain banned. Both original coaches remain enabled and eligible. The sixth login is **not yet banned**; its workspace is already revoked, so it cannot obtain new pooling authority. Its private credentials must not be shared. Existing issued access tokens have their own expiry; workspace revocation is the immediate pooling boundary.

The single remaining approval requested is to sign out and disable ONLY this run-created engineering login, remove only its test allowlist entry, and revoke only its exact fixture manifest, notice and scope grant. Preserve all history, backups, original accounts and the two original coaches' test window. No deletion, global disablement or Production change is proposed. The script checks exact run ownership and original coach preservation before mutation. Its projection assertion was corrected to the backend's actual event status `stop`, not the Classic workout label `stopped`.

This final receipt and that operational-script correction are saved in a local follow-up commit only. The tested application code is pushed and deployed at `10dea53`; pushing the evidence-only follow-up would trigger an eleventh Preview build, outside the approved cap. No application source changed after the tested deployment.

## Remaining release gates

The original 110-case map remains 57 passing engineering invariants, 43 partial and 10 external-scope blocked; these extra A31 checks do not automatically reclassify whole acceptance cases. Broader hosted races/failure cases, every builder/import path, exact accepted domain outputs, full accessibility/performance and participant UAT remain visible in [remaining work](./REMAINING_WORK.md).

Professional exercise/clinical/rights acceptance, business/privacy/market decisions, off-device/full hosted recovery and separate main/Production/real-client release approval remain pending. Owner PRD/catalogue approval is recorded but does not fabricate specialist signatures. No real-client launch, autonomous assignment or rehabilitation clearance is claimed.
