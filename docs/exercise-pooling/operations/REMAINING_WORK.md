# Pooling work remaining

Updated 7 September 2026 during approved A29 local implementation. **Work in progress, not release-ready.** Classic remains preserved. No remote push, hosted access or real-client assignment is authorized.

## Locally verified portions

- PostgreSQL 16.15 now runs from an official, integrity-checked, project-local read-only image. Docker is no longer blocking native SQL tests. Synthetic auth claims are **not** hosted JWT verification.
- Foundation/authority/gateway/source/projection and legacy-boundary SQL tests passed. `native-flow.mjs` passes confirmed sources → trusted JS decision → exact coach approval → client start → explicit zero actual → stop, including incorrect-unit rejection.
- `native-concurrency.mjs` passed all-or-none batch rollback/retry and health-change serialization against approval/start at its intermediate version. Rerun after subsequent migrations.
- `native-extensions.mjs` passes source-backed R2 arithmetic → reviewed-dose match → coach acceptance as an **unassigned** draft; client privacy, original targets, retry and stale rejection after a health report are covered. Policies are fictional engineering inputs.
- 91 distinct Node unit tests and lint pass. UI build passed before the latest catalogue administration additions; rerun final build.
- In-app browser: corrected source labels, explicit synthetic zero save and reload persistence pass; current preview logs showed no errors. Broader changed-screen/mobile/accessibility coverage remains.
- Classic archive/bundle restoration and earlier synthetic fallback checks remain historical evidence for their recorded versions, not full post-build recovery proof.

## Authored/integrated, with verification still required

- Coach-only canonical editor, exact-decision/diff review, single/batch approval and narrow assigned-session projection.
- Write-ahead operation journal with Web Locks, actor/client isolation, original-request retry, reload recovery and definitive rejection records.
- R2/R3 backend review requests, numerical service/Edge adapter, effect/progression/reassessment primitives, reviewed-dose mapping and accept/amend/reject lifecycle.
- Versioned catalogue validator and service-only acceptance-hash publication/revocation. SQL creation passed; behavior, revocation races and complete reviewer tooling remain.

## Remaining local implementation and tests

1. Scoped restrictions, explicit resolution and processing-purpose/consent admission ledger. Current authoritative `held` guard remains fail-closed; source bundles still lack resolved restriction integration. Ordinary confirmations cannot clear holds.
2. Finish source integration and every legacy/copy/template/voice/client path, including client-safe reports/history. R1 client portal currently uses a limited protected view rather than the full Classic feature set.
3. Finish runner per-side/load/effort/history/correction UI and robust stop/offline/unknown-outcome handling. Additional SQL validation needs complete negative/concurrency coverage.
4. Finish R3 numerical-history integration, explicit reassessment workflow and constraint-aware weekly composition UI/persistence. Batch approval is not the entire weekly-planning feature.
5. Complete catalogue review/admission/revocation tooling and tests without publishing real candidates or inventing evidence.
6. Exercise Edge entrypoints using authorized isolated Deno if needed. Hosted Supabase auth, REST and storage remain separately unverified.
7. Rerun native role/source/transaction tests on a fresh full additive-schema database; add correction, expiry, consent/restriction, revoked-content, failure and concurrent-operation cases.
8. Complete post-build backup/restore and feature-off Classic compatibility rehearsal preserving new sidecars, targets and actuals.
9. Complete browser/visual/mobile/keyboard coverage, current performance measurements, sprint reconciliation and final evidence.

## External gates — still excluded

- Named professional content/clinical/rights acceptance and actual approved numerical parameters.
- Operator/privacy/market facts and applicable review; real coach/client acceptance.
- Exact hosted staging target, live backup/recovery proof, pilot/release permission.
- Remote push, merge, deployment, real-client enablement and off-device backup require separate scope/evidence.

Continue independent local work under A22–A29 without requesting routine sprint approval again. Remaining implementation is not automatically “blocked” merely because release sign-offs are pending.
