# A31 — seven-day fictional coach-test handoff

8 September 2026. **Protected fictional workflow verified on Preview 9; final candidate/engineering-account retirement checks follow this commit. Not a real-client release.**

## Preserved boundaries

Global database R1/R2/R3 flags remain false. Only a newly created, explicitly enrolled fictional workspace can enable the test workflow. Existing clients/profile roles, main, Production and Fitness Scribber Classic are preserved. The 48 real candidates remain unpublished. Professional, clinical, privacy and participant acceptance remain separate.

The two existing coach accounts may create their own new fictional workspace through normal login. At most one per coach; fixed expiry **15 September 2026, 16:07:56 IST / 10:37:56 UTC**. Revocation cannot be undone from the UI. Expiry/revocation blocks new actions, preserves the enrolled marker, and retains results and Stop. Do not turn on global flags for this handoff.

The sixth/final synthetic identity exercised fresh hosted client-role denials, then only that new fictional identity was provisioned as a coach for browser testing. No original role/password changed. Five retired identities remain banned. Retire the sixth after verification without disabling existing coaches' entry.

## Verified so far

- Private PG17 archive: 707,482 bytes; SHA-256 `08e4d30cf37d6c74aafcba210ff2dd6af806d29f063f2732f41404362e04eaee`.
- All **83 public/auth/storage table row fingerprints** match a fresh native restore. An initial ledger-only mismatch was locale ordering; explicit `COLLATE C` matched every row without changing data. Managed Auth/REST/Edge/provider infrastructure is not recreated by native restoration.
- Exact migration rehearsed twice before hosted application. Migration SHA-256 `edc05a8aea3ab87825ffafbfda21c89b88793b7d88ca0ae4e6c014ca66c61bdb`; transaction SHA-256 `95d259a96a8cf99da22b3a7b68a8e8348dbeedea19ae7992d3f73407e5ec9cbd`. Existing rows and 18 previous migrations preserved; globals off.
- Twelve native checks: acknowledgement, Classic behavior, wrong owner/client role, private config, immutable identity, manifest isolation, source/decision gateways, Stop reserve, expiry, corrections, non-renewable revocation and globals off.
- 141 unit tests, lint and build pass; existing large-bundle warning remains. Lost-response preparation reuses exact requests, generations and operation keys. Weekly choices exclude stale, superseded and already assigned drafts without deleting history.
- Hosted JWT denies client workspace creation, self-promotion and config changes; normal coach JWT reaches the entry point.
- Browser against hosted backend: normal login, explicit fictional creation, backend scenario, unassigned draft, generation, compatible swap, exact validation/approval, Start, zero actual, Pause, fresh-answer Resume and completion verified. Reload preserved the original zero, the second performed set and an attributed correction (three records).
- R2 hosted coach JWT: already completed occurrences return no change. An unstarted baseline produces the exact fictional 1-to-2-second proposal; acceptance saves an unassigned draft without rewriting the baseline.
- R3 hosted coach JWT: completed comparable history produces the exact fictional progression; acceptance remains an unassigned draft. The setup helper now preserves reviewed occurrence identities for later comparable scenarios.
- Protected Preview 9, commit `26e1d92`: normal application sign-in and two-session weekly review/explicit batch approval verified. Both exact sessions appear as assigned in the fictional client's persisted session list; an older generation is visibly stale. Approval remained disabled until the explicit review acknowledgement.
- Read-only preservation after these journeys: **711 pre-A31 rows across 83 tables unchanged**, 101 additional rows in those tables plus two new infrastructure rows; one workspace used 109 writes. No Storage objects or real published candidates. Conservative application-row bound including both owners' future workspaces is 940, below the 1,000-row cap.

## Final verification and accounting

The tenth/final permitted Preview build is reserved for this candidate: clearer exact draft IDs, comparable-history preparation, current weekly choices, tests and the [coach guide](./COACH_PREVIEW_TEST_GUIDE.md). Only the three exact-branch Preview browser flags are true; Production variables are unchanged. The backend global flags remain false. Preview 9's immutable URL is https://fitness-scribber-kq6i-cyy6dqwzm-cureocity1.vercel.app.

After deployment, the final browser pass must verify preparation, exact approval, execution, early workspace revocation, Stop and correction/history preservation before retiring only the engineering identity. The private run ledger records the actual final SHA/URL, browser result, retirement and repeat preservation. This commit does not pre-claim those future results; the final user handoff must wait for them. Do not create an eleventh Preview build to publish that final receipt.

The owner keeps existing coach access; no email, invitation, membership change or protection bypass is required. Both existing coaches can create a NEW fictional workspace, one each, through the guide. The fixed window is not renewed by creation, refresh or revocation. Six lifetime test identities have now been created; no seventh is allowed. At completion all six engineering identities must be banned, while the two original coaches remain allowlisted until the fixed deadline.

## Corrections and remaining limitations

The R2 harness initially used an obsolete assignment property and then expected a completed occurrence to change. Those expectations were corrected; the preserved failure records are not relabelled as passes. R3 correctly refused an unmatched occurrence identity; the comparison scenario now explicitly preserves the reviewed baseline selection while requiring fresh source/decision/approval. Browser ISO text filling did not populate the native date field; verified native date entry did. No date policy or backend validation was bypassed.

The 110-case acceptance map is not closed by these additional checks. Broader adverse hosted races, every import/builder route, exact accepted domain parameters, full accessibility/performance, actual-client UAT and professional/privacy/release acceptance remain in [remaining work](./REMAINING_WORK.md). Owner PRD/catalogue approval is not an invented specialist signature. Same-device relational restore is not an off-device/full managed-service recovery certificate.

No main push, Production promotion, protection bypass, invitation, paid resource, real training or real-content publication is authorized.
